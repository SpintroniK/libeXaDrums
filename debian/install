# This unprefixed debhelper file affects the first package in
# debian/control, that is the library package
# (this trick avoids hard-conding the soversion).
# The joker matches a file and a symlink.

usr/lib/${DEB_HOST_MULTIARCH}/libexadrums.so.*
